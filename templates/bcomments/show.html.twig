{% extends 'base.html.twig' %}

{% block title %}Bcomment{% endblock %}

{% block body %}
    <h1>Bcomment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bcomment.id }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ bcomment.contenu }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bcomments_index') }}">back to list</a><hr>

    <a href="{{ path('app_bcomments_edit', {'id': bcomment.id}) }}">edit</a>

    {{ include('bcomments/_delete_form.html.twig') }}
{% endblock %}
