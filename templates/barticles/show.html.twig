{% extends 'base.html.twig' %}

{% block title %}Barticle{% endblock %}

{% block body %}
    <h1>Barticle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ barticle.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ barticle.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ barticle.description }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ barticle.author }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_barticles_index') }}">Back to list</a>

    <a href="{{ path('app_barticles_edit', {'id': barticle.id}) }}">Edit</a>

    {{ include('barticles/_delete_form.html.twig') }}
{% endblock %}
