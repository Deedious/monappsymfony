{% extends 'base.html.twig' %}

{% block title %}Bcategories{% endblock %}

{% block body %}
    <h1>Bcategories</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bcategory.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ bcategory.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bcategories_index') }}">back to list</a>

    <a href="{{ path('app_bcategories_edit', {'id': bcategory.id}) }}">edit</a>

    {{ include('bcategories/_delete_form.html.twig') }}
{% endblock %}
