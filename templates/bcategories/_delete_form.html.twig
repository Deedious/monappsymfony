<form method="post" action="{{ path('app_bcategories_delete', {'id': bcategory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bcategory.id) }}">
    <button class="btn">Delete</button>
</form>
